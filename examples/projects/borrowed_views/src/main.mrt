module main
import views;

fn main() -> i32 {
    var record: views.Record = views.Record { number: 5 };
    print(views.view_record_relay(record).number);
    views.edit_record_relay(record).number = 8;
    print(record.number);
    return 0;
}
