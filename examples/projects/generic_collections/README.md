# generic_collections

Acceptance project for monomorphized `Vec<T>` support, including scalar values,
generic structs, owned `Buffer` elements, allocator compatibility preflight,
and zero-copy transfer of vectors containing owned values.

```bash
merit-project verify examples/projects/generic_collections
```
