# Typed result application

Demonstrates payload enums, exhaustive matching, `try` propagation, and public module exports.

```bash
merit-project verify .
merit-project run .
```

Expected output:

```text
42
10
20
```
