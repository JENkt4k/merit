# M6 test data

`alpha1_corpus_v1.json` is the canonical Alpha.1 same-source semantic convergence manifest. It is consumed by `tests/bootstrap/test_alpha1_corpus_convergence.py` and is deliberately separate from `bootstrap_corpus_v1.json`, which proves the bootstrap lexer/expression representation contract rather than the complete Alpha.1 replacement semantic surface.
