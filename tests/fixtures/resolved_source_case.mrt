module demo
enum Choice { Left(i64), Right(i64), Other(i64) }
capability clock;
fn main()->i64 { let a:Resource=1; let flag:i64=1; match flag { Left(x) => { drop(a); return 1; } Right(y) => { drop(a); } Other(z) => { drop(a); } } with capability clock { return 2; } }
